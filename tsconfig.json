{
  "compilerOptions": {
    /* Type checking is the only goal here. bun runs the .ts sources directly,
       so we never emit JS. */
    "noEmit": true,
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",

    /* Allow `import "./foo.ts"` with an explicit .ts extension, which is how
       the sources import each other under bun. */
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "verbatimModuleSyntax": false,

    /* Strictness */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,

    /* Interop / runtime */
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["node", "bun"]
  },
  "include": ["cli/**/*.ts", "shared/**/*.ts", "image/**/*.ts", "test/**/*.ts"],
  "exclude": ["node_modules"]
}
